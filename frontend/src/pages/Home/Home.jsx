// import Slider from "../components/Slider";
import Section from "../../components/common/Section/Section.jsx";
// import { products } from "../utils/products";

const Home = () => {
    // Example filtering by category
    // const newArrivals = products.filter((item) => item.category === "Vehicle");
    // const bestSales = products.filter((item) => item.category === "Something-Else");

    return (
        <>
            <p className="text-xl text-center font-semibold text-blue-600">This is Home Page</p>

            {/*<Slider />*/}
            {/*<Section title="Big Discount" products={products.slice(0, 4)} />*/}
            {/*<Section title="New Arrivals" products={newArrivals} />*/}
            {/*<Section title="Best Sales" products={bestSales} />*/}

        </>
    );
};

export default Home;
